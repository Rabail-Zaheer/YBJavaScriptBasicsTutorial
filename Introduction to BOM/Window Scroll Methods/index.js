"use strict";

/* scrollBy() and scrollTo() methods */

// scrollBy()

function scrollWinby() {
  window.scrollBy(-20, 0);
  // the first value is x axis and the second value is y axis
}

// scrollTo
function scrollwinTo() {
  window.scrollTo(0, 50);
}
